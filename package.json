{
  "name": "git-sync",
  "displayName": "Git Sync",
  "description": "Sync settings, keybindings, and extensions across workspaces using GitHub",
  "version": "1.0.0",
  "publisher": "cursor-sync",
  "repository": {
    "type": "git",
    "url": "https://github.com/james-tempus/cursor-settings-sync.git"
  },
  "engines": {
    "vscode": "^1.60.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:gitSync.wrapWithIgnore",
    "onCommand:gitSync.wrapWithFocus",
    "onCommand:gitSync.setupGitHubSync",
    "onCommand:gitSync.exportSettings",
    "onCommand:gitSync.importSettings",
    "onCommand:gitSync.syncSettings"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "gitSync.wrapWithIgnore",
        "title": "Git Sync: Wrap with Ignore"
      },
      {
        "command": "gitSync.wrapWithFocus",
        "title": "Git Sync: Wrap with Focus"
      },
      {
        "command": "gitSync.setupGitHubSync",
        "title": "Git Sync: Setup GitHub Sync",
        "category": "Git Sync"
      },
      {
        "command": "gitSync.exportSettings",
        "title": "Git Sync: Export Settings"
      },
      {
        "command": "gitSync.importSettings",
        "title": "Git Sync: Import Settings"
      },
      {
        "command": "gitSync.syncSettings",
        "title": "Git Sync: Sync Settings"
      }
    ],
    "keybindings": [
      {
        "command": "gitSync.wrapWithIgnore",
        "key": "ctrl+alt+i",
        "mac": "cmd+alt+i",
        "when": "editorTextFocus"
      },
      {
        "command": "gitSync.wrapWithFocus",
        "key": "ctrl+alt+f",
        "mac": "cmd+alt+f",
        "when": "editorTextFocus"
      },
      {
        "command": "gitSync.exportSettings",
        "key": "ctrl+alt+e",
        "mac": "cmd+alt+e"
      },
      {
        "command": "gitSync.importSettings",
        "key": "ctrl+alt+m",
        "mac": "cmd+alt+m"
      },
      {
        "command": "gitSync.syncSettings",
        "key": "ctrl+alt+s",
        "mac": "cmd+alt+s"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/vscode": "^1.60.0",
    "@types/glob": "^7.1.3",
    "@types/mocha": "^8.2.2",
    "@types/node": "14.x",
    "eslint": "^7.27.0",
    "@typescript-eslint/eslint-plugin": "^4.26.0",
    "@typescript-eslint/parser": "^4.26.0",
    "glob": "^7.1.7",
    "mocha": "^8.4.0",
    "typescript": "^4.3.2",
    "vscode-test": "^1.5.2"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
